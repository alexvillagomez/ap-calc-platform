/**
 * /calc-onboarding — the original AP Calc / Precalc onboarding demo experience.
 * Preserved here so nothing is lost. The new landing at / routes to /math or /mcat.
 */
import Onboarding from "@/components/Onboarding";

export default function CalcOnboardingPage() {
  return <Onboarding />;
}
